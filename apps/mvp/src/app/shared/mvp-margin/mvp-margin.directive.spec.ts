import { MvpMarginDirective } from './mvp-margin.directive';

describe('MvpMarginDirective', () => {
  it('should create an instance', () => {
    const directive = new MvpMarginDirective();
    expect(directive).toBeTruthy();
  });
});
