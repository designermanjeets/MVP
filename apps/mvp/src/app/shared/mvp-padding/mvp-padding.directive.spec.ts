import { MvpPaddingDirective } from './mvp-padding.directive';

describe('MvpPaddingDirective', () => {
  it('should create an instance', () => {
    const directive = new MvpPaddingDirective();
    expect(directive).toBeTruthy();
  });
});
